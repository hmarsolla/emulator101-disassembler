# emulator101-disassembler

A Python 8080 processor disassembler based on [Emulator101](http://www.emulator101.com/) guide.

Created as a study of low level logic.

```shell
python disassembler.py ROM_FILE
```